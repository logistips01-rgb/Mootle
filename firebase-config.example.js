// Copia este archivo a `firebase-config.js` y pega los datos de tu app web de
// Firebase (Console → Configuración del proyecto → Tus apps → Web </> → Config).
// firebase-config.js está en .gitignore: tus claves no se suben y no choca al
// hacer git pull.
export const firebaseConfig = {
  apiKey: "TU_API_KEY",
  authDomain: "TU_PROYECTO.firebaseapp.com",
  projectId: "TU_PROYECTO",
  storageBucket: "TU_PROYECTO.appspot.com",
  messagingSenderId: "TU_SENDER_ID",
  appId: "TU_APP_ID",
};
